// Set Interval

function oneMoreHello() {
    console.log("Hello Subhranil !");
}

setInterval(oneMoreHello, 1000); // will print after every 1 sec

// Set Timeout

setTimeout(oneMoreHello, 5000); // will print only after 5 sec